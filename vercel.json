{
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "npm run migrate:deploy --workspace=packages/db && npm run build --workspace=apps/web",
  "outputDirectory": "apps/web/.next",
  "regions": ["gru1"]
}
